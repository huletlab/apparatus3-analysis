/*
 * Project:  Testing Fermi-Dirac functions
 *
 * Author:   Pedro M Duarte 2011-02
 * 
 */

#include "funcs.h"
#include "math.h"

using namespace std;

bool VERBOSE;

int
main (int argc, char **argv)
{

  VERBOSE = false;

  printf ("Fermi-Dirac  F1(10.) = %.6f\n", f1 (10.));
  printf ("Fermi-Dirac  F2(-0.133) = %.3e\n", f2 (-0.13388));
  printf ("Fermi-Dirac F32(10.) = %.6f\n", f32 (10.));

  /*for (int i = -100; i < 100; i++)
     printf ("%f\t%f\t%f\n", i * 1.0, f0 (i * 1.0) / fm1 (i * 1.0),
     fq (i * 1.0)); */
  //printf ("Fermi-Dirac  F1(-100.) = %.6f\n", f1 (-100.));
  return 0;
}
